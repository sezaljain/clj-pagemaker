(ns converter.pmd-collector)

;; this is the final document
;; contains pages,color,font,pagewidth,pageheight,doublesided etc
;; you can add pages with a new id
;; you add shapes to a page
;; common through the document - -page width, page height, doublesided, color and fonts
